# 展覽網站規格書

> 這份文件的用途：帶進 Claude Code 專案資料夾，作為背景資料，讓 Claude 讀取後直接建立 `claude.md` 並開始動工，避免來回溝通誤差。

---

## 1. 網站目的

這是一場**展覽**在線上的呈現——展覽本身以兩棟建築案為展出內容，網站等於是這場展覽的數位版本。調性是「策展／展覽」，不是建築師事務所的作品集，展期／日期不是重點，不需要特別強調。同時要能長期、輕鬆地自行維護內容——新增／下架參展建築案、調整視覺風格，不需要每次都重新請人改程式碼。

## 2. 核心需求摘要

- ✅ 正式可上線（有網域、可部署）
- ✅ 展出項目可自由上架／下架（不需要碰程式碼）
- ✅ 具備「多自訂美術界面設定」——後台可調整配色、排版風格等視覺參數
- ✅ 整體調性：策展／展覽感，編輯雜誌式排版，圖文並置，帶互動細節

## 2.1 風格方向（精神參考，非逐一複製）

質感方向參考 [朋丁 pon ding](https://www.ponding.tw/) 這類藝文展覽網站的策展編輯氣質——**這是感受層級的精神參考，不是要複製它的版型**。核心精神：

- 無卡片框線、無明顯網格，版面自由、非制式化
- 圖文並置的編輯雜誌感，左右並行的非對稱排列
- 每個展出項目可以有自己獨立的視覺語言，不必套用同一套模板
- 帶一點微妙的互動細節（hover、輕微動效），但不花俏

> 實際的字體選擇、色彩搭配、版面細節這類「精細質感」的執行，建議直接在 Claude Code 或 Claude Design 裡進行——那裡有完整字體與 CSS 自由度，能真正做到參考網站的質感，不受限於基礎預設樣式。

## 3. 建議技術架構

| 項目 | 建議 | 原因 |
|---|---|---|
| 前端框架 | Next.js | 效能好、SEO 友善、部署容易 |
| 資料庫／後台資料存放 | Supabase | 免費額度夠用，內建簡易的身份驗證(登入後台) |
| 部署 | Vercel | 免費、跟 Next.js 整合度高、更新即時生效 |
| 圖片存放 | Supabase Storage 或 Cloudinary | 作品圖片上傳與自動優化 |
| 動畫函式庫 | Framer Motion | 跟 Next.js 搭配度高，做進場／轉場動效比純 CSS 精緻好控制 |

> 上下架、配色、排版設定都存在資料庫裡，前台網頁會即時讀取，因此改動不需要重新寫程式或重新部署。

## 4. 網站頁面架構（展覽網站，二展品結構）

只有兩件展出項目，因此**不採用「首頁精選＋作品列表＋About」的一般作品集架構**，改為以展覽為主體的編輯式頁面：

1. **展覽首頁** — 極簡導覽列，下方兩棟建築案左右並行呈現，各自獨立的圖文編排（不套同一套模板），可標示建築案名稱／地點等基本資訊，不強調展期日期，不做制式化的強視覺 Hero。**兩棟建築案直接完整呈現在首頁上，不做分類、標籤或篩選選單這類中介層，使用者一進首頁就能同時看到兩者，不需要多點一次才看到內容**
2. **建築案頁 × 2** — 每棟建築案各自獨立一頁，內容包含：
   - 大尺寸圖像（可多張、全螢幕捲動）
   - 名稱、地點、年份、類型等基本資訊
   - 論述文字（簡短精準）
   - 圖說／技術圖說明（如有）
3. **關於／展覽介紹** — 可整合為頁尾一小段文字，或極簡的單一區塊
4. **後台管理（需登入）**
   - 新增／編輯建築案（僅作為未來擴充彈性；目前只有兩案，前台不套用分類／篩選介面）
   - 一鍵切換建築案「上架／下架」狀態（上架中的項目才會出現在首頁）
   - 美術設定面板：
     - 主色調／輔助色
     - 字體風格
     - 圖文版面比例／排列方式
     - 微互動效果開關（hover、輕微動效）

## 5. 視覺風格方向（策展編輯感，精神參考朋丁 pon ding）

- 無卡片框線、無明顯網格線，版面自由排列，避免制式化
- 大尺寸圖像為主角，圖片本身要有品質
- 兩棟建築案左右並行，各自可有不同的字體／版面處理，展現各自的個性，不強求統一模板
- 圖文並置的編輯雜誌感，善用留白與非對稱排列
- 帶微妙的互動細節（hover 效果、輕微動效），但不花俏
- 確切的字體、色彩、排版比例等執行細節，留到 Claude Code／Claude Design 裡實作時再精修

## 5.1 動畫效果建議

動效的角色是引導閱讀節奏、強化策展氛圍，不是炫技，整體要維持克制：

1. **進場淡入** — 圖片與文字在頁面載入或捲動進入視窗時，以極短的淡入＋些微位移出現（例如向上位移 20px、淡入 0.6–0.8 秒），內容依序出現，塑造閱讀節奏
2. **頁面轉場** — 從首頁點進建築案頁面時，圖片以連續放大的方式銜接成下一頁主視覺（shared element transition），比單純切換更流暢，也呼應「線上展覽」的沉浸感
3. **懸停細節** — 連結文字底線緩慢展開、圖片極輕微縮放（scale 1.02 左右）即可，避免誇張的彈跳或旋轉
4. **視差捲動** — 大圖背景以略慢於內容的速度捲動，增加空間深度感，用量克制，避免造成暈眩
5. **導覽微互動** — 選單展開/收合、頁面間切換保持俐落的過渡，不拖沓

## 6. 給 Claude Code 的下一步指示

1. 開啟 Claude Code（桌面版或終端機皆可），建立一個新的專案資料夾
2. 把這份文件放進資料夾，連同你的作品圖片素材
3. 要求 Claude Code：「讀取這份規格書，建立 claude.md 並初始化 Next.js + Supabase 專案結構」
4. 依規格書第 4 節的頁面架構，逐頁建立
5. 最後串接 Supabase 後台管理介面，並部署到 Vercel

---

*備註：實際開發時如遇到規格書未涵蓋的細節（例如確切色票、字體檔案、作品數量），可直接在 Claude Code 對話中補充，不需要重新定義整體架構。*
