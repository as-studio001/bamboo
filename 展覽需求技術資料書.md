# 展覽需求技術資料書

> 本文件取代先前分散在對話中的美術／互動需求討論，是目前唯一有效的設計依據。與根目錄的〈展覽網站規格書.md〉若有衝突，以本文件為準（本文件是規格書之後、實作前的細化與修正）。
>
> 狀態：**草稿，待使用者確認**。確認前不會動工。

---

## 1. 展覽定位與調性

- 線上策展，只收錄兩個建築案（A、B），不是作品集、不是電商
- 整體質感要專業、像藝術網站／畫廊，而非事務所官網
- 參考質感來源：GAMUT 海報系列（粗黑海報字、大膽色塊）、獨立書籍裝幀設計（書背索引＋內容並置的邏輯）——**取的是排版邏輯與質感，不是逐一複製印刷品的視覺元素**（例如不做黑色印刷邊框、不做翻書動畫）
- 整體質感要偏**雜誌式編排**（參考 Werkschau Thurgau 展覽網站的雙分割頁面：照片區塊與純色資訊區塊並置、小字索引列表），避免落入制式化、樣板感的版型；目前試作版本被使用者評為「太制式化」，之後的調整都要往「更像雜誌排版」的方向修正
- 另一個重要參考：ETH Studio Meili Vogt Conzett 網站（meili.arch.ethz.ch）——靜態雙欄並置、大圖＋粗黑標題＋日期、內文段落＋「Mehr Informationen」連結、欄間用「+」符號分隔、左右邊緣有重複的直書標題文字。桌面版拿掉主從切換動畫後，這個參考的「純靜態雙欄雜誌感」會是接下來調整的方向

## 2. 核心互動機制（本次最關鍵的架構決策）

### 2.1 頁面結構

- ~~單一首頁體驗，沒有獨立的建築案頁面（先前規劃的 `/projects/[slug]` 路由取消）~~
- ~~不需要可單獨分享／深連結到某一案展開狀態的網址~~
- **（2026-08-25 推翻上面兩點，改回有獨立案例頁）** 首頁雙欄降級成安靜的索引頁，只留封面、一串「圖片＋簡短說明」（見 2.6c）跟基地地圖；完整論述跟章節內容改到 `/projects/[slug]` 案例內頁——每案一個獨立、可深連結分享的網址，內頁排版邏輯直接參考使用者另一個專案 Internal-Pages 的 `render.js` 模式（hero → 主要論述 → 幾個「更詳細」子章節 → 頁尾地圖）。細節見 2.6c／2.8。
- 兩案在首頁同一個頁面裡並列呈現，各自獨立捲動；案例內頁則是每案各自一個完整頁面

### 2.2 預設狀態

- A、B 兩案並列呈現，**各佔 50% 版面寬度**，同時存在、形成「對話」
- **（已修正）兩案改成各自獨立捲動，各有各的捲軸**——原本規劃的「共用同一條捲軸、內容同步往下捲」已取消。改成獨立捲動是為了強化兩案的分界感：原本兩欄版型完全對稱時分界不明顯，讓使用者可以各自往下捲、捲動位置各自不同，視覺上會更明確是「兩個獨立案子」而不是一份連續內容
- 整個雙欄區塊固定佔滿一個螢幕高度（100dvh），每一欄自己的內容（開場影片＋章節）在這個固定高度裡自己捲動，捲軸視覺上隱藏（功能還在，只是不顯示那條軸）
- 頁尾已拿掉（見第 6 節），頁面本身不會再往下捲出雙欄區塊以外的內容

### 2.3 桌面版邏輯（已修正：取消主從切換）

- **桌面版不再有主從切換動畫**，兩案固定 50/50 靜態並列，滑鼠移動不會改變版面比例
- 比較接近 Meili Vogt Conzett 工作室網站那種靜態雙欄雜誌排版：兩欄各自完整呈現，沒有互動式的寬度變化
- ~~原本「滑鼠停在哪一側、哪一側展開為 80%」的機制~~ 已移除，**只保留在手機版**（見 2.4）

### 2.4 手機版切換邏輯（唯一保留主從切換的裝置層級；觸控裝置沒有 hover，改用點擊鎖定）

- 預設同樣 50/50 並排（維持「兩案並立」原則，不因螢幕小而改成上下堆疊或單一次呈現一案）
- 點擊 A 欄 → A 展開為主要（比例可能比桌面更極端，約 85/15，因手機寬度更窄，主要欄需要更多空間才讀得動）
- 點擊 B 的窄條 → 切換為 B 主要、A 收窄
- 屬於「離散鎖定」而非連續反應，避免觸控滑動誤觸切換

### 2.5 次要／窄條狀態要顯示什麼

- 改為**直書標籤式**呈現（參考 TRANSIT 雜誌網站右側那排直向書背標籤的質感），不是先前橫向排列「案名＋一列照片」的版本
- 案名以直書文字呈現，貼近分界處，仍可搭配照片，但版面邏輯要像「收起來的標籤／書背」而非簡化版的完整版面

### 2.6 章節內容結構（六類分類不變，但已搬到案例內頁——見 2.6c／2.8）

六個章節分類仍是（**2026-08-18 定案，取代原本的基地／構造／材料／使用情境四類**）：

1. 建築設計（Architectural Design）
2. 模矩（Module）
3. 構造（Structure）
4. 施工（Construction）
5. 組裝說明書（Assembly Manual）——額外可放 **PDF 下載**（`chapter.pdf`）
6. 基地（Site）——額外顯示**地圖**標記案子位置（見 2.6b）

**（2026-08-25 修正）章節的完整文字與圖片，已經從首頁搬到案例內頁展開**，不再是「首頁雙欄捲動時依序出現」：

- **建築設計** → 內頁的主要論述（不顯示自己的標題，就是開頭那段敘述文字），圖文節奏維持原本「圖—文—圖—文」穿插（大圖→內文→小圖→內文…）
- **模矩／構造／施工／組裝說明書** → 內頁的「more in detail」子章節，**這次標題要顯示**（跟以前首頁刻意隱藏標題不同），標題＋段落文字＋一排縮圖
- **基地** → 內頁最下方，顯示地圖（跟首頁那份地圖是同一個 `ChapterMap` 元件、各自獨立掛載）
- 章節標籤列從首頁頂部**移到案例內頁左側**（桌面版 sticky 直向清單／手機版收成頂部一排可橫向捲動的標籤），點擊用原生 `scrollIntoView({behavior:'smooth'})` 捲到內頁對應區塊——不再是「同步捲動兩案」，因為現在是各案各自一個獨立頁面

**每個章節都可以插入影片**（`chapter.video`）：**尺寸略小於圖片、左右刻意留白**，用原生 `<video controls>` 播放，瀏覽器內建的全螢幕按鈕即可放大到全螢幕。目前兩案都還沒有實際影片檔，欄位留空時不會顯示影片區塊。

### 2.6a 組裝說明書的 PDF 下載

「組裝說明書」章節額外有一個 `chapter.pdf: { url, label }` 欄位，有值時在章節內文下方顯示一個底線文字下載連結（螢光綠字）。目前兩案都還沒有 PDF 檔案，欄位留空。

### 2.6b 基地章節的地圖

「基地」章節額外顯示**衛星影像地圖**標記案子位置，讀取 `project.coordinates: { lat, lng }`。

**（已修正）改用 Leaflet 自己疊圖磚，取代原本的 OpenStreetMap embed**：OpenStreetMap 的免金鑰 embed 只有一般街道地圖樣式、沒有衛星影像選項。**（再修正，2026-08-19）圖磚來源改成國土測繪中心 PHOTO2**（`wmts.nlsc.gov.tw`），跟使用者另一個專案 Internal-Pages 同一個圖資來源——台灣官方測繪資料，比先前用的 Esri World Imagery 更貼合台灣基地的精細度，同樣**不需要 API key、不需要註冊帳號**。案子位置用水滴形圖標＋脈動白環（造型照抄 Internal-Pages 的 `.site-pin`，顏色換成本站的螢光綠點綴色），而不是 Leaflet 預設圖釘（預設圖釘的圖檔路徑在 Next.js 打包後會解析失敗）。座標欄位 `project.coordinates: { lat, lng, address?, zoom? }` 對齊 Internal-Pages 的 `map` 欄位格式。

**目前兩案都還沒有實際座標**，架構已經做好並實測過衛星圖磚能正常載入，UI 會顯示「地圖待補（尚未提供座標）」的虛線框佔位提示；等使用者提供實際地址或經緯度後，只要填入 `coordinates` 欄位地圖就會自動顯示，不用再改版面程式碼。

### 2.6c 首頁的照片索引（2026-08-25 新增，2026-08-26 修正圖片來源）

首頁雙欄拿掉六章節的長文字論述後，改成一串「圖片＋簡短說明」的清單，風格參考 herzogdemeuron.com 的 process 頁面：

- **（已修正）照片不是獨立的 `project.photos` 扁平清單，而是直接借用每個章節自己的 `chapter.images`**——這樣每張照片天生就知道自己屬於哪個章節，頂部的章節標籤才能準確捲到對應的照片群組（同一個 `id={`${slug}-${key}`}` 錨點，跟案例內頁共用同一套章節 key）。「基地」章節不算進照片索引，那一節在首頁跟內頁都是用地圖代表
- **一張一列，不並排**，每張照片用原生 `<img>`（不是 `next/image`）顯示，讓照片維持自己的原始比例——直式就直式、橫式就橫式，**不套用統一形狀的裁切遮罩**；照片本身比封面小一號、左右留白（不滿版貼邊），圖說文字置中對齊
- 沒有 `src` 的佔位圖維持固定比例（4:3）的灰階色塊＋說明文字，避免版面在補圖前後跳動
- 每張圖片外層保留 `data-snap`，慣性捲動停下來後一樣會吸附對齊（沿用 2.2 節提到的捲動機制）；每張照片整塊都是連到該案例內頁的連結
- 封面（開場首圖）跟基地地圖**都保留**在首頁雙欄，不受這次簡化影響，分別在照片索引的前面／後面
- **頂部標籤列同步回到「同頁捲動」邏輯**：點一個章節標籤，A、B 兩案的對應照片群組會同步捲進視窗（沿用 2.2 節的 `animateScrollTo`），不是換頁連結——因為每張照片都能定位到自己的章節，這裡才能準確捲動。左右邊框維持「2026林鐵構築」品牌文字不變

### 2.7 轉場方式

- **明確排除**：翻書式／書頁翻動動畫
- 改用網頁原生常見的轉場語彙來詮釋「主從切換」與「揭露下層資訊」，例如寬度／比例的平滑動畫、淡入淡出、輕微縮放。動效本身要克制，服務於「切換版面份量」這個功能，不是炫技

### 2.8 案例內頁 `/projects/[slug]`（2026-08-25 新增）

架構直接參考使用者另一個專案 Internal-Pages（`architecture-case-study-template`）的 `js/render.js` 排版順序：

1. **Hero**：滿版封面圖＋疊字標題／地點年份類型 meta，跟首頁開場首圖同一張封面照片、同一套自動配字色邏輯（`useImageTextColor`）
2. **左側章節導覽**（直書，貼在左側邊框，取代該側的「2026林鐵構築」）：建築設計／模矩／構造／施工／組裝說明書／基地，點擊用原生 `scrollIntoView({behavior:'smooth'})` 捲到內頁對應區塊
3. **建築設計**：內頁主要論述，圖—文—圖—文穿插（沿用首頁原本 `ChapterBlock` 的節奏）
4. **模矩／構造／施工／組裝說明書**：more-in-detail 子章節，標題（這裡會顯示）＋段落文字＋一排縮圖；組裝說明書子章節另外顯示 PDF 下載連結
5. **基地**：頁尾顯示地圖（跟首頁那份地圖各自獨立掛載，不共用狀態）

版面是一般文件從上捲到下（不是首頁雙欄那種固定 100dvh 各自捲動），跟首頁共用同一套白色三周留白＋右側直書「2026林鐵構築」邊框（左側邊框改放章節導覽，見上）。`EdgeTitle` 用 `fixed` 定位，`side` 參數控制只畫哪一側。

### 2.9 全站分頁式導覽（2026-08-26 試做後撤回）

~~仿瀏覽器分頁的視覺語彙，全站最上方常駐兩個分頁（兩案各自名稱），總覽不獨立成分頁，內頁的分頁用實色背景「長」在內容上面、帶「×」關閉回首頁~~——做出來後使用者覺得不好看，撤回這個方向，改回原本的做法：頂部標籤列還是章節分類（見 2.6 節的 `TopChapterNav`），不是案名分頁；左右邊框在首頁維持兩側都是「2026林鐵構築」。案例內頁本身的左側章節導覽（2.8 節）不受影響，那是更早就定案、獨立於這次分頁列實驗的設計。

### 2.10 案例內頁改成彈出式資訊視窗（2026-08-26 新增）

首頁點照片不再整頁跳轉，改成蓋一層彈出視窗在首頁上方，用 Next.js App Router 的 **Parallel Routes ＋ Intercepting Routes**（`src/app/@modal/(.)projects/[slug]/page.tsx` 攔截從「/」出發的點擊）：

- 首頁維持掛載在背景（不會被卸載），視窗接近全螢幕但四周留一圈間距（`DetailModal.tsx`：`pt-16 sm:pt-24` 讓頂部空間給首頁既有的章節標籤列，其餘三邊也留白），讓雙欄首頁在背後半透明、模糊可見
- 視窗質感是 Apple「Liquid Glass」那種半透明毛玻璃：`backdrop-blur-2xl backdrop-saturate-150` 加上 `globals.css` 新增的 `--glass-bg`／`--glass-border`（rgba 值直接寫進變數裡，不是用 Tailwind 的顏色透明度修飾語法——那個語法對 CSS 變數沒用，`ModeTabs` 那次「active 分頁跟頁面背景同色、直接消失不見」的 bug 就是這樣來的）
- 章節標籤列不重複做一份，沿用首頁原本就有的 `TopChapterNav`——首頁沒被卸載，這排標籤本來就還在畫面上；`ProjectDetail` 新增 `showChapterNav` prop，彈出視窗版本傳 `false`，不重複顯示自己的 `ChapterEdgeNav`（那是 `fixed` 定位，放進視窗裡會跑到螢幕最左邊、跑出玻璃卡片外面）
- 關閉用 `router.back()`（點空白處／右上角 × ／Esc 都是），回到「/」時首頁捲動位置不會被重置
- **直接訪問／分享網址／重新整理 `/projects/[slug]` 不受影響**，還是原本的完整獨立頁面（`src/app/projects/[slug]/page.tsx`，自己的左側章節導覽），這是 intercepting routes 的標準行為：只有從「/」出發的 client-side 導覽才會被攔截成彈出視窗
- **已知簡化**：視窗開啟時，首頁的 `TopChapterNav` 點擊還是控制首頁自己的照片群組捲動，不會連動捲動視窗內部的內容——兩者是各自獨立的捲動容器，之後如果需要「標籤同時知道該捲哪一個」的智慧判斷，要另外處理

## 3. 色彩（已修正：黑白＋照片原色＋單一螢光綠點綴）

- 整體走**黑白＋照片原色**（參考 Meili Vogt Conzett），兩案共用同一套中性色調，不再各自用滿版深色／淺色綠當背景
- **點綴色改為單一螢光綠**（參考 Liquid Architecture 網站，hex `#39FF14`），用在「+」符號、「更多資訊」連結、年份／地點等小標籤文字這些細節上，不當滿版底色
- **A、B 兩案共用同一個螢光綠，不再分深淺**——兩案的分界已經靠「各自獨立捲動」（見 2.2）處理，顏色不需要再兼顧區隔辨識的責任
- 螢光綠是目前定案色票，若後續要微調可再提供更精確的 hex 值
- **圖片一律滿版貼齊欄位左右邊緣，不留邊距**；只有文字段落（標題、內文、meta）有左右內距。這是為了避免整塊版面看起來都套同一種留白，呼應 Meili 參考圖「圖片出血、文字內縮」的邏輯
- **新增深色模式**：站在淺色模式之外，另外新增一套可切換的深色配色（見 3.1），不是取代淺色模式
- **外框留白**：整個雙欄區塊的上／左／右三邊留一圈白色外框（桌面 30px、手機 18px，同比例），下面貼齊不留白（那是捲動延續的方向）；全站底色也從米色改成純白，跟面板白色統一
- **標題整併進邊框**：原本浮動疊加、靠 mix-blend-difference 反色的導覽列拿掉了，標題文字改成「**2026林鐵構築**」，以直書文字分別貼在左／右白色邊框裡（參考 Meili Vogt Conzett 邊界直書標題的處理），不再是獨立的導覽列元件

### 3.1 深色模式

- Nav 列右側加了一個文字切換按鈕（「深色模式」／「淺色模式」），點擊切換全站配色
- 預設會先讀使用者上次的選擇（存在瀏覽器 localStorage），沒有紀錄的話跟隨系統的深色／淺色偏好設定
- 深色模式：面板底色改為近黑、文字改為近白；綠色點綴色目前維持同一組色票（尚未針對深色模式微調對比，色票定案後可能需要重新檢查深色模式下的可讀性）
- 目前只套用在首頁（展覽主頁面），後台管理頁面尚未跟進深色模式

## 4. 字體

- 走**粗黑海報式**（參考 GAMUT），大字、粗體、視覺衝擊力強
- 中文字體目前先預設用思源黑體最粗字重（Noto Sans TC Black），使用者若有指定字型再替換

## 5. 分界與框架

- **不做黑色粗邊框**當結構元素（先前參考書封／海報圖片裡的黑色印刷邊框不需要保留）
- 兩案之間只需要有清楚的視覺分界（可能是留白、細線，或色彩本身的對比），不需要額外裝飾性邊框

## 6. 頁尾（已移除）

- ~~「關於展覽」小段文字~~ 已拿掉，目前頁面不設頁尾。雙欄區塊固定佔滿一個螢幕高度，頁面本身不會再往下捲出任何東西

## 7. 開場首圖（Hero）與進場流程

- 目前「案名＋基本資訊」純文字表頭太平淡。改為：**進入畫面時，各案的第一個畫面是一支開場影片，案名直接疊在影片上**——文字與影像整合成海報式構圖，不是文字獨立一塊、圖片再接在後面
- 參考來源：CAORA（咖啡烘豆品牌網站，開場是滿版自然風景背景影片，品牌名疊在上面，播幾秒後才切到可捲動的內容頁）、動畫工作室網站（巨大標題疊在圖片下緣，索引編號＋分類標籤置於畫面角落）——**只取「開場影片＋文字疊圖」與「播完才能往下捲」這套流程邏輯，不是要複製參考圖的鮮豔色彩**
- **目前流程（已取消捲軸鎖定）**：一進站，A／B 兩案 50/50 並列，各自在自己的版面裡顯示開場影片（影片內容待使用者另外提供）；**不鎖捲軸**，使用者隨時可以直接往下捲，開場影片區塊就是頁面最上方的第一個區塊，往下捲自然接續進到章節內容，往上捲可以捲回開場影片
- ~~原本規劃「影片播完前鎖住捲軸、播完才解鎖」的機制~~ 已取消
- **標題文字顏色自動判斷**：不加漸層／陰影這類裝飾，改成直接分析封面照片「底部區域」（標題疊放處）的平均亮度，自動決定標題用白字還是深字（`src/lib/useImageTextColor.ts`）。這是特意做成「一勞永逸」的方案——封面照片是暫定的，之後很可能會換，換照片後顏色會自動跟著重新計算，不用每次手動調
- **開場多圖快速輪播**：`coverImages` 裡有幾張真實照片就刷幾張，1.5 秒內快速播完、停在最後一張當作持續存在的封面。作品一目前已有 10 張照片在輪播（略過還沒補圖的灰色佔位，不會刷到）。使用者預計每案提供約 10 張照片；目前每案只上傳了 1 張，所以還不會有輪播效果，等照片補齊會自動動起來，不用再改程式碼

## 8. 尚待確認／待補（不影響先出骨架版，但正式上線前需要回頭補齊）

- [x] ~~實際色票~~：點綴色已定案為螢光綠 `#39FF14`（A、B 共用同一色），但**深色模式下的對比尚未檢查**——螢光綠背景是近黑色時，可讀性需要再確認
- [ ] 圖片處理風格：目前佔位圖是中性灰階色塊；使用者這次提到「照片原色」，代表正式圖片應該用彩色照片本身的顏色，不套濾鏡——等真實圖片到位後直接確認即可，不需要額外套用效果
- [ ] 開場影片檔案（使用者稍後提供），目前是佔位骨架
- [ ] **章節標題的呈現方式**：使用者要找範本給參考，目前先隱藏不顯示
- [x] ~~兩案分界方式~~：改成兩案各自獨立捲動解決（見 2.2），不用再加分界線或鏡射對齊
- [x] ~~導覽列~~：拿掉浮動導覽列，標題改成「2026林鐵構築」直書整併進左右白色邊框（見 3 節）
- [x] ~~章節分類架構~~：2026-08-18 定案為六類——建築設計／模矩／構造／施工／組裝說明書／基地（取代原本的基地／構造／材料／使用情境），見 2.6
- [ ] 兩案的真實資料：案名、地點、年份、類型、各章節的圖片與文字內容——**已開始補**：案名已定案（作品一「常民竹小屋」、作品二「一籌」），兩案的開場封面照片已上線；地點／年份／類型與六個章節的圖文還沒補
- [x] ~~圖片上傳~~：後台加了拖拉上傳介面（`/admin`），把圖片存進 `public/images/{slug}/`，透過 `src/app/api/upload/route.ts` 這支 API 寫檔。**這是開發階段的暫時做法**——正式部署到 Vercel 後檔案系統唯讀，這個端點會失效，屆時要換成 Supabase Storage 之類的雲端物件儲存
- [ ] 每個章節的影片檔案（`chapter.video`）——架構已做好（略小於圖片、左右留白、原生全螢幕），兩案六章目前都還沒有實際影片
- [ ] 組裝說明書的 PDF 檔案（`chapter.pdf`）——架構已做好（下載連結），兩案都還沒有實際 PDF
- [ ] 基地章節的實際座標（`project.coordinates`）——架構已做好（OpenStreetMap embed，免金鑰），目前顯示「地圖待補」佔位，等使用者提供地址／經緯度
- [ ] 後台管理介面還要繼續補——目前有「上下架＋美術設定＋圖片上傳」，之後要能編輯六個章節（建築設計／模矩／構造／施工／組裝說明書／基地）與開場首圖各自的文字內容、上傳影片與 PDF、填入地圖座標，也還沒有深色模式、登入驗證，且圖片上傳完後還沒有介面能把圖片指定給對應的章節（目前要手動編輯 `src/lib/projects.ts` 才能接上）
- [ ] 章節內容份量定案後，回頭調整每章捲動節奏與頁面總長度

## 9. 明確排除的方向（避免重工）

- 不做翻書／書頁翻動動畫
- 不做黑色印刷邊框當結構裝飾
- ~~不做獨立的建築案分頁或可深連結的個別網址~~（2026-08-25 推翻，見 2.1／2.8：現在有 `/projects/[slug]` 案例內頁）
- 不做無卡片框線以外的裝飾性容器（沿用原規格書「無明顯網格、無卡片框線」的精神）
- 兩案顏色不用大對比色相區分（後續走深淺對照，仍是同一套邏輯下的克制選色）
- 章節內容捲動時不做逐一淡入位移的動畫（先前試作版本因此造成視覺干擾而移除），僅開場首圖允許一次性進場動態

---

*下一步：使用者確認本文件內容無誤後，才開始依此重寫首頁的互動與版面邏輯。*
